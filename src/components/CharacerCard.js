import React from 'react';

const CharacterCard = (props) => (
    // Individual Character 
    <div className="">
        <h2>{}</h2>
        <p>{}</p>
        <p>{}</p>
    </div>
)
export default CharacterCard